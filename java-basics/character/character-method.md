⏺️ ➡️ 🟦 🔵 🟢🔴⭕🟠🟣🟥🟧✔️ ☑️ • ‣ → ⁕

# ⏺️ Character method

### ➡️ Character.isLetter(char ch)

- Checks if the character is an alphabet.

```java
Character.isLetter('A');   // true
Character.isLetter('1');   // false

```

### ➡️ Character.isDigit(char ch)

- Checks if the character is a number.

```java
Character.isDigit('5');    // true
Character.isDigit('a');    // false

```

### ➡️ Character.isLetterOrDigit(char ch)

- Checks if it is either a letter or digit.

```java
Character.isLetterOrDigit('9');   // true
Character.isLetterOrDigit('#');   // false

```

### ➡️ Character.isWhitespace(char ch)

- Checks if the character is space, tab, newline, etc.

```java
Character.isWhitespace(' ');   // true
Character.isWhitespace('\n');  // true

```

### ➡️ Character.isUpperCase(char ch)

- Checks if character is uppercase.

```java
Character.isUpperCase('A');  // true
Character.isUpperCase('a');  // false

```

### ➡️ Character.isLowerCase(char ch)

- Checks if character is lowercase.

```java
Character.isLowerCase('a');  // true
Character.isLowerCase('A');  // false

```

### ➡️ Character.toUpperCase(char ch)

- Converts character to uppercase.

```java
Character.toUpperCase('a');  // 'A'

```

### ➡️ Character.toLowerCase(char ch)

- Converts character to lowercase.

```java
Character.toLowerCase('A');  // 'a'

```

### ➡️ Character.getNumericValue(char ch)

- Returns numeric value of a character.

```java
Character.getNumericValue('9');  // 9
Character.getNumericValue('A');  // 10

```

### ➡️ Character.isAlphabetic(int codePoint)

- Checks if character is alphabetic (Unicode supported).

```java
Character.isAlphabetic('A');  // true
Character.isAlphabetic('₹');  // false

```

### ➡️ Character.valueOf(char ch)

Converts primitive `char` into `Character` object.

```java
Character c = Character.valueOf('J');

```

### ➡️

```java

```

### ➡️

```java

```

### ➡️

```java

```
